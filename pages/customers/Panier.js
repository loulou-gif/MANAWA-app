import { View,ScrollView, Text } from 'react-native'
import React from 'react'

const Panier = () => {
  return (
    <ScrollView>
      <Text>Panier</Text>
    </ScrollView>
  )
}

export default Panier